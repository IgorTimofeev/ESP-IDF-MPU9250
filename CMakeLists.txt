cmake_minimum_required(VERSION 3.22)

if(ESP_PLATFORM)
    file(GLOB_RECURSE YOBA_SOURCES
        "src/MPU9250.cpp"
    )

    idf_component_register(
        SRCS ${YOBA_SOURCES}
        INCLUDE_DIRS src
        REQUIRES ESP-IDF-BusHAL
    )

    target_compile_options(${COMPONENT_LIB} PUBLIC -std=gnu++26)

    return()
endif()